# .coveragerc

[run]
# This is the key fix: forces coverage to use relative paths.
relative_files = true
# Tells coverage to measure everything in the current directory.
source = .

[report]
# Excludes the test files from the coverage report itself.
omit =
    test_*.py